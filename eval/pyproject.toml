[project]
name = "metaygn-eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["benchmarks"]
asyncio_mode = "auto"
